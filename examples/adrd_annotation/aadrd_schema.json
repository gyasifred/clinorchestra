{
  "type": "object",
  "properties": {
    "severity": {
      "type": "string",
      "enum": ["no_impairment", "MCI", "mild_dementia", "moderate_dementia", "severe_dementia", "insufficient_data"],
      "description": "Cognitive functional status per NIA-AA: MCI = preserved function (CDR 0.5), dementia = impaired function (CDR ≥1)"
    },
    "syndrome": {
      "type": "string",
      "enum": ["amnestic", "executive", "language", "visuospatial", "behavioral", "mixed", "insufficient_data"],
      "description": "Predominant cognitive-behavioral syndrome pattern based on affected domains"
    },
    "diagnosis": {
      "type": "object",
      "properties": {
        "classification": {
          "type": "string",
          "enum": ["ADRD", "Non-ADRD"]
        },
        "specific_type": {
          "type": "string",
          "enum": ["alzheimers_disease", "vascular_dementia", "lewy_body_disease", "frontotemporal_dementia", "mixed_dementia", "other_adrd", "delirium", "psychiatric", "medical_condition", "other_non_adrd"]
        },
        "confidence": {
          "type": "string",
          "enum": ["high", "moderate", "low"]
        }
      },
      "description": "Etiological diagnosis with classification, specific type, and confidence level"
    },
    "clinical_summary": {
      "type": "string",
      "description": "Integrated clinical narrative: severity rationale (functional assessment), syndrome characterization (affected domains), etiology reasoning, and differential considerations"
    }
  },
  "required": ["severity", "syndrome", "diagnosis", "clinical_summary"]
}
